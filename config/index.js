import firebase from '../config/firebase';
import FirebaseContext from '../context/firebaseContext';

/* Exporting the FirebaseContext as a named export. */
export { FirebaseContext };

export default firebase;
